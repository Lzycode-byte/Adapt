import 'package:flutter/material.dart';

final kToday = DateTime.now();
final kFirstDay = DateTime(kToday.year, kToday.month, 1);
final kLastDay = DateTime(kToday.year, kToday.month + 1, 0);

final kColor = Colors.green;
